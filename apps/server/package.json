{
  "name": "server",
  "scripts": {
    "dev": "dotenv -e .env -- tsnd  --project tsconfig.json --respawn --transpile-only ./app.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "ts-node-dev": "^2.0.0"
  }
}
