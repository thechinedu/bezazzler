{
  "name": "bezazzler",
  "packageManager": "yarn@3.5.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "sdks": "yarn dlx @yarnpkg/sdks"
  },
  "devDependencies": {
    "turbo": "^1.9.3",
    "typescript": "^5.0.4"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "dotenv-cli": "^7.2.1",
    "plugin": "^0.3.3"
  }
}
